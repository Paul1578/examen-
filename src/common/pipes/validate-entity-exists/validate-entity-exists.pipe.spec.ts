import { ValidateEntityExistsPipe } from './validate-entity-exists.pipe';

describe('ValidateEntityExistsPipe', () => {
  it('should be defined', () => {
    expect(new ValidateEntityExistsPipe()).toBeDefined();
  });
});
